// lib/core/constants/breakpoints.dart

/// Screen-width breakpoints used to switch between mobile and web/desktop
/// layouts (e.g. bottom nav vs side nav).
abstract final class Breakpoints {
  static const mobile = 600.0;
  static const compactRail = 1000.0; // below this: icon-only side nav
}
